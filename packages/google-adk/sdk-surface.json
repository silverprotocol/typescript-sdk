{
  "$comment": "Ratchet manifest for the fixture-drift gate (scripts/check-fixture-drift.mjs), google-adk facet (the google-adk-ratchet task, 2026-07-03 \u2014 the third facet wired into the gate, after claude-agent-sdk + openai-agents). UNLIKE the other two facets, google-adk does NOT import its upstream SDK at runtime \u2014 `packages/google-adk/src/index.ts`'s `AdkEvent`/`AdkPart` are a HAND-TYPED PROJECTION of the verified wire (the file header cites google/adk-python's `Event`/`LlmResponse` classes + the Gemini `Content`/`Part` REST shapes; `@google/adk` is only an OPTIONAL peerDependency, never imported). So the 'inventory' this manifest ratchets is not 'does the facet's own import surface still match' (there is no such import) but 'does the INSTALLED reference SDKs' actual field vocabulary still match what the hand-typed contract assumes' \u2014 the two ground truths available in this workspace (both resolved via packages/e2e's `@google/adk` devDependency pin; `@google/genai` is TRANSITIVE, not a direct e2e dependency, resolved in a second hop scoped to @google/adk's own installed subtree, mirroring the openai-agents manifest's agents->agents-core hop): (1) `partKind` \u2014 the Gemini `Part` interface's own field names, from `@google/genai`'s bundled `dist/genai.d.ts` (the part-kind vocabulary this facet's `driveAdkPart` if-chain switches on); (2) `eventField` \u2014 the `Event`/`LlmResponse` interface field names, from the OFFICIAL `@google/adk`'s `dist/types/events/event.d.ts` + `dist/types/models/llm_response.d.ts` (`Event extends LlmResponse`; both interfaces' OWN fields are flattened into one inventory, since the hand-typed `AdkEvent` interface itself makes no such split). Dispositions were seeded 2026-07-03 by reading `driveAdkPart`/`driveAdkTopLevel`/`maybeCloseTurn` line-by-line against every installed field. handled = the facet has a dedicated, correct branch (or is genuinely CONSUMED for correlation, e.g. `id` in `turnKey()`); carried = routed losslessly to the existing `provider-raw` content.block carry (mirrors `driveAdkTopLevel`'s pre-existing `unmappedActions`/`unmappedEvent` named-field-ledger pattern \u2014 NOT a generic reflect-over-keys catch-all); not-applicable = provably cannot be a meaningful JSON-wire value on this facet's ingestion boundary; silently-dropped = a genuine, disclosed gap left un-landed (see below). FIVE findings were closed by carries: on `partKind`, ADDING the unmapped-part-fields carry (a new, unconditional check at the TOP of `driveAdkPart`, checked BEFORE the kind-specific if-chain's early returns \u2014 necessary because `videoMetadata` normally rides ALONGSIDE an already-matched `inlineData`/`fileData` part per genai's own doc, so a check placed only in an 'else' fallback would never see it) closed `mediaResolution`/`videoMetadata`/`toolCall`/`toolResponse`/`partMetadata`; on `eventField`, extending the PRE-EXISTING `unmappedEvent` bag in `driveAdkTopLevel` closed `candidateIndex` (absent from the hand-typed contract entirely) and `branch` (already typed on `AdkEvent` but never read). TWO findings were seeded, investigated, then DELIBERATELY left `silently-dropped` rather than landed: `author`/`timestamp` are ALSO real, currently-unread `AdkEvent` fields present on every real event (on the ORIGINAL @iqai/adk `Event` class both were REQUIRED; on the official @google/adk 1.3.0 `Event` interface `timestamp` remains required and `author?` is optional in the TYPE but still set on every event the runner appends in practice \u2014 the empirical rationale is unchanged, unlike the genuinely-occasional `candidateIndex`/`branch`) \u2014 empirically, folding them into the generic carry fired a provider-raw content.block on every single native event in packages/e2e's captured adk fixtures (`author:\"spike\"` is set on all events of both corpus scenarios), breaking the recorded golden `*.agjson.json` snapshots and the cross-framework convergence assertions; regenerating those cassette fixtures is outside this ratchet's boundary (facet + manifests + script + SPEC \u00a78 only). See their own per-entry notes below and `index.test.ts`'s explicit negative-control test. SPEC \u00a78 item 23 documents the FOUR carries that DID land (`candidateIndex`/`branch` + the five `partKind` fields). See .superpowers/sdd/adk-ratchet-report.md for the full rationale + wire citations. RETARGETED 2026-07-13 (official-SDK migration): the eventField ground truth moved from `@iqai/adk` 0.1.22's bundled class declarations to the OFFICIAL `@google/adk` 1.3.0's interface files, and the inventory was re-verified line-by-line against the facet: `candidateIndex`/`error` left the SDK surface (entries removed; the facet's candidateIndex carry stays as harmless tolerance for older wire), `citationMetadata` was already carried and `inputTranscription`/`outputTranscription` already handled (entries added), `modelVersion` is newly carried (ledger arm added on retarget), and `goAway`/`liveSessionResumptionUpdate`/`liveSessionId` are live/bidi session control-plane fields disposed not-applicable (never populated on the `runner.runAsync` ingestion boundary this facet normalizes).",
  "verified": [
    {
      "sdkVersion": "1.3.0",
      "date": "2026-07-07",
      "silverprotocol": "0.3.1",
      "evidence": "live validation: launch/validation/e2e-adk-google*.mjs (text + tool turns; upstream google/adk-js#475 diagnosed to root cause and filed)"
    },
    {
      "sdkVersion": "1.3.0",
      "date": "2026-07-08",
      "evidence": "LIVE captures: multi-turn + single-tool-call + text-only + tool-error corpus (gemini-2.5-flash) \u2014 captured directly on the public typescript-sdk mirror by a parallel migration effort; adopted into the workspace 2026-07-13 (native+provenance verbatim, agjson/coverage regenerated through the current facet) and landed as standing replay/census seeds"
    },
    {
      "sdkVersion": "1.3.0",
      "date": "2026-07-13",
      "silverprotocol": "0.3.3",
      "evidence": "drift-gate eventField re-seed against the official interfaces (24 members) + e2e harness migration off @iqai/adk; live conformance capture pending"
    },
    {
      "sdkVersion": "1.3.0",
      "date": "2026-07-13",
      "silverprotocol": "0.3.3",
      "evidence": "LIVE conformance capture: echo-gemini35 corpus (gemini-3.5-flash, official MCPToolset over Streamable HTTP) \u2014 landed as a standing replay/census seed; census caught + fixed per-turn usage summation (intermediate rounds' usageMetadata incl. thoughtsTokenCount now summed into turn.done)"
    },
    {
      "sdkVersion": "1.3.0",
      "date": "2026-07-22",
      "silverprotocol": "0.3.3",
      "evidence": "day-one gemini-3.6-flash validation: live text + tool-loop e2e clean, census-grade capture landed as standing replay seed (corpus echo-gemini36) with native field-set diffed against the gemini-3.5-flash baseline \u2014 zero new wire fields, all dispositions transfer, replay gate green"
    },
    {
      "sdkVersion": "1.4.0",
      "date": "2026-07-22",
      "silverprotocol": "0.3.5",
      "evidence": "fixture suite + drift gate + full 1.3.0->1.4.0 d.ts diff audit: exactly ONE new Event/LlmResponse wire field \u2014 interactionId (opt-in Interactions API identity; carried via the unmappedEvent provider-raw ledger per the modelVersion precedent, with mirror test) \u2014 all other deltas config/tooling-plane (hasThoughts/pruneThoughts helpers, RunnerConfig app/resumabilityConfig, CompositeSessionKey refactor). genai 1.52.0->2.x MAJOR diffed clean: Part/Content/FunctionCall/FunctionResponse/GenerateContentResponseUsageMetadata byte-identical at 2.12.0 AND 2.13.0 (partKind verifiedAt advanced to the installed 2.12.0). google/adk-js#478 (fix for the role-less-newMessage 400 we filed as #475) verified SHIPPED in 1.4.0's dist (runner.js defaults role to 'user', esm :113-114) AND live-confirmed: a deliberately role-less newMessage survived a full gemini-3.6-flash tool-loop replay"
    },
    {
      "sdkVersion": "1.4.0",
      "date": "2026-07-22",
      "silverprotocol": "0.3.5",
      "evidence": "LIVE capture: echo-gemini36 standing seed REFRESHED at 1.4.0 (gemini-3.6-flash, genai 2.12.0 underneath) \u2014 replay + census gates green (zero untriaged drops, zero new fields); interactionId absent from wire as expected (harness never enables useInteractionsApi)"
    },
    {
      "sdkVersion": "1.4.0",
      "date": "2026-07-25",
      "silverprotocol": "0.3.6",
      "evidence": "FIRST adk MCP-Apps capture (workspace#2): live gemini-3.6-flash render_card via the official MCPToolset \u2014 corpus app-spec-gemini36 landed as a CI-standing replay seed; the census caught functionResponse.response's MCP-Apps SIBLINGS (structuredContent + _meta.ui.resourceUri) silently dropped by the content-array branch \u2014 FIXED: functionResponseToToolDoneFields now carries both onto tool.done's first-class \u00a72.1 channels (converging with the openai facet's extractStructuredContent precedent), mirror tests added, cassette regenerated through the fixed facet, replay + census gates green"
    },
    {
      "sdkVersion": "1.5.0",
      "date": "2026-08-04",
      "silverprotocol": "0.3.9",
      "evidence": "1.4.0->1.5.0 (+genai 2.12.0->2.15.0) tarball-diff audit + synthetic-frame tests: Event/LlmResponse own fields UNCHANGED (24-member eventField inventory transfers; gate green); the two real wire additions both carried \u2014 (1) CompactedEvent gains isScratchpad (anchored compactor scratchpad, PR #470; subtype file, outside the ratcheted inventory \u2014 whole projection carried via the unmappedEvent provider-raw ledger as session-replay tolerance, true/false/absent + author-suppression mirror tests, never reachable on the runAsync boundary), (2) genai Part gains audioTranscription (FIRST new part kind since the 14-member ratchet; carried whole via the unmapped-part-fields provider-raw block per the videoMetadata sibling precedent, mirror + 2.12.0-shaped byte-identical negative controls; Reducer folds clean, no parks). Non-wire: event.d.ts re-export relocation (AF_FUNCTION_CALL_ID_PREFIX/generateClientFunctionCallId/populateClientFunctionCallId/getEventTokens \u2014 behavior identical) and A2A-only fixes (errorCode metadata, contentless content: undefined \u2014 the facet has no A2A surface). Pin bumped; live re-capture pending in this cohort's capture step."
    },
    {
      "sdkVersion": "1.5.0",
      "date": "2026-08-04",
      "silverprotocol": "0.3.9",
      "evidence": "Live captures landed (echo-gemini36 + app-spec-gemini36 @ gemini-3.6-flash, adk 1.5.0 / genai 2.15.0): replay + census + INV-MSG fold gates all green with ZERO new census findings \u2014 the 1.5.0 wire at this facet's runner.runAsync boundary matches the tarball audit exactly. audioTranscription remains synthetic-only (no native-audio-output scenario exists in the corpus; disclosed in the carry entry above)."
    },
    {
      "sdkVersion": "1.6.0",
      "date": "2026-08-08",
      "silverprotocol": "0.4.1",
      "evidence": "wire-no-op minor (parked from the 2026-08-08 ai-trio slice, studied same day): full d.ts inventory diff 1.5.0->1.6.0 + genai 2.15.0->2.16.0 against the drift gate's own consumed surfaces \u2014 genai Part 15 fields unchanged (the surface that forced the 1.5.0 study via audioTranscription: zero delta this time), adk Event 7 own fields unchanged, LlmResponse 18 fields unchanged. 1.6.0's additions are all non-wire (a2a/auth, get_user_choice_tool + request_input_tool \u2014 registerable tools whose invocations ride the EXISTING functionCall/functionResponse envelope \u2014 crypto/error/output_schema utils). e2e pins bumped exact (adk 1.6.0, genai 2.16.0), single-copy lockfile resolution verified, 918 tests incl. corpus replay + fold gates green. Nightly's forced-latest adk leg has been green all week (no [peer-compat] ever filed). No new corpus scenario demanded \u2014 no wire surface changed to capture."
    },
    {
      "sdkVersion": "1.6.0",
      "date": "2026-08-13",
      "silverprotocol": "0.4.2",
      "evidence": "genai 2.16.0->2.17.0 MINOR study (adk itself unchanged at 1.6.0): Part interface zero-delta (15 fields, tarball d.ts diff + drift-gate-verified post-install) \u2014 wire-no-op; partKind section verifiedAt -> 2.17.0, e2e genai pin 2.17.0."
    },
    {
      "sdkVersion": "1.6.0",
      "date": "2026-08-15",
      "silverprotocol": "0.4.4",
      "evidence": "genai 2.17.0->2.17.1 PATCH study (adk itself unchanged at 1.6.0): Part interface byte-identical (tarball d.ts diff) - wire-no-op; partKind section verifiedAt -> 2.17.1, e2e genai pin 2.17.1."
    },
    {
      "sdkVersion": "1.6.0",
      "date": "2026-08-19",
      "silverprotocol": "0.5.1",
      "evidence": "gemini-3.7-flash day-6 LIVE validation trio (GA 2026-08-13): echo + app-spec (MCP structuredContent/_meta.ui carry re-attested) + thinking (FIRST real Gemini thought:true capture \u2014 thinkingLevel scenario knob -> thinkingConfig.includeThoughts, 2 thought parts + signatures -> reasoning.start/delta/end + reasoning.opaque). All three enrolled as replay seeds at capture time; census clean after [*].content.parts[*].thought triage (structurally-consumed flag, [*].partial precedent). New finish-reason mappings (UNEXPECTED_TOOL_CALL, TOO_MANY_TOOL_CALLS + lossy message.metadata raw carry) ship in the same cohort."
    },
    {
      "sdkVersion": "2.0.0",
      "date": "2026-09-02",
      "silverprotocol": "0.5.4",
      "evidence": "@google/adk 1.6.0->2.0.0 MAJOR (npm 2026-08-21; nightly [peer-drift] typescript-sdk#22) + @google/genai 2.17.1->2.20.0 - tarball d.ts audit independently re-derived: every listed breaking change (LLMAgentWrapper removal, InvocationContext.agent optional, BaseAgent extends BaseNode, Sequential/Parallel/Loop deprecation, rerunOnResume, workflow engine #588) lives on the new workflow/node plane; LlmResponse byte-identical; Event gains four optional workflow-plane fields (output/route/nodeInfo/isolationScope, stamped only by workflow/*.js and vertex_ai_session_service round-trips - never on a plain LlmAgent runAsync stream) -> carried into the event-level provider-raw ledger (CompactedEvent/interactionId precedent); EventActions gains agentState (object; contract widened string->JsonValue, JsonValue.parse'd) + endOfAgent (already carried); genai Part gains ONE field, mediaProcessing (request-side media hint; MEDIA_PROCESSING_UNSPECIFIED|STATIC|AGENTIC) -> unconditional top-of-part carry beside mediaResolution/videoMetadata; FinishReason (18), Content, Candidate, ThinkingConfig/ThinkingLevel, UsageMetadata, FunctionCall/FunctionResponse IDENTICAL. Dependency shape: @modelcontextprotocol/sdk and the five @mikro-orm drivers + @google-cloud/storage + GCP OTel exporters became OPTIONAL peers loaded lazily (591->172 packages) - consumers using MCPToolset must install @modelcontextprotocol/sdk themselves; the lockfile's mikro-orm/sqlite3/tar subtree left (17 dev-only dependabot alerts closed); adm-zip stays ^0.5.17 (google/adk-js#601; workspace override to ^0.6.0) and the 0.205.0 otlp-http exporters still exact-pin @opentelemetry/core 2.1.0 (override ^2.8.0). Capture agent (InMemoryRunner/LlmAgent/MCPToolset/runAsync) typechecks and runs unchanged. LIVE: the standing gemini-3.7-flash trio RE-CAPTURED at 2.0.0/2.20.0 (echo/app-spec/thinking; app-spec first - it drives MCPToolset through the new optional-peer loader): census ZERO drops / ZERO new fields on all three, thinking-gemini37 still yields thought:true parts + signatures. Peer range widened >=1.0.0 <2 -> <3 (floor kept: 1.x and 2.x Event shapes normalize through one facet; vercel-ai's two-major range is the precedent); e2e pins adk 2.0.0 + genai 2.20.0 exact; stale exclusions pruned. Nightly leg 'force @google/adk@2.0.0' was already green (run 33564341176). Consumers compiling with skipLibCheck:false against adk 2.0.0 should know its root barrel type-imports the now-optional @google-cloud/storage (TS2307) - upstream shape, not ours."
    }
  ],
  "sections": {
    "partKind": {
      "$comment": "The Gemini `Part` interface's own field names (`@google/genai`'s `dist/genai.d.ts`, `export declare interface Part { \u2026 }`) \u2014 the part-kind vocabulary `driveAdkPart` switches on. `thought`/`thoughtSignature` are MODIFIER fields (ride alongside `text`/`functionCall`/`functionResponse`, never a kind of their own), listed here as their own members because they are still distinct installed field names the gate must track. RE-VERIFIED 2026-07-22 (adk 1.4.0 peer bump): @google/adk 1.4.0 moves its @google/genai dependency across the 1.x->2.x MAJOR (declares ^2.9.0; installed resolution 2.12.0) \u2014 the extracted `Part` interface block is byte-identical across 1.52.0/2.12.0/2.13.0 (and Content/FunctionCall/FunctionResponse/GenerateContentResponseUsageMetadata carry doc-comment rewording only), so every disposition below transfers unchanged; verifiedAt advanced to the installed 2.12.0, with the newest published 2.13.0 diffed identical. RE-VERIFIED 2026-08-04 (adk 1.5.0 / genai 2.15.0 peer bump): the 2.12.0->2.15.0 diff adds exactly ONE `Part` field \u2014 `audioTranscription?: Transcription` \u2014 the FIRST new part kind since this section was ratcheted at 14 members (now 15; entry below). The `Transcription` interface itself gains `speakerLabel?: string` + `words?: WordInfo[]`, with NEW `WordInfo {word?, startOffset?, endOffset?}` (duration-string offsets) \u2014 sub-object shape, in-scope here only through the audioTranscription entry's note. All 14 prior dispositions transfer unchanged. RE-VERIFIED 2026-09-02 (adk 2.0.0 / genai 2.20.0 peer bump): the 2.17.1->2.20.0 diff adds exactly ONE `Part` field \u2014 `mediaProcessing?: MediaProcessing` ('How the model processes this part's media for understanding.'; enum MEDIA_PROCESSING_UNSPECIFIED | STATIC | AGENTIC) \u2014 the section grows 15 -> 16 members (entry below). Content/Candidate/FinishReason (18 values)/ThinkingConfig+ThinkingLevel/UsageMetadata/FunctionCall/FunctionResponse are IDENTICAL 2.17.1 -> 2.20.0. All 15 prior dispositions transfer unchanged.",
      "sdkPackage": "@google/genai",
      "verifiedAt": "2.20.0",
      "members": {
        "text": {
          "disposition": "handled",
          "note": "Primary text content -> text.start/delta/end (with citations/providerMetadata.google.thoughtSignature when signed, audit M22/\u00a78.8). When the SAME part also has `thought:true`, it routes to reasoning.* instead (see the `thought` entry) \u2014 `driveAdkPart` checks `thought` FIRST and returns before reaching the text branch, so a thought+text part is never double-emitted."
        },
        "thought": {
          "disposition": "handled",
          "note": "Boolean modifier, checked FIRST in driveAdkPart's if-chain -> reasoning.start/delta/end + reasoning.opaque{kind:'signature'} when `thoughtSignature` is also set."
        },
        "thoughtSignature": {
          "disposition": "handled",
          "note": "Per-part base64 replay signature (\u00a78.8, echo-or-400). Rides FOUR places depending on which sibling field is set on the SAME part: reasoning.opaque (thought parts), text.start/end.providerMetadata.google.thoughtSignature (signed non-thought text, e.g. Google-Search-grounded turns), tool.args.assembled.signature (functionCall parts), and tool.done.providerMetadata.google.thoughtSignature (functionResponse parts)."
        },
        "functionCall": {
          "disposition": "handled",
          "note": "tool.start + tool.args.delta + tool.args.assembled. Null-id minting (audit M47, window-scoped multiset dedup for the partial:false aggregate re-send \u2014 see ToolCallMintState's file-header doc) when `functionCall.id` is absent, as it often is on the Developer API."
        },
        "functionResponse": {
          "disposition": "handled",
          "note": "tool.done via functionResponseToAgBlocks() (MCP-resource-shape content[] passthrough, or a typed `data` block for a plain object result). Null-id correlation resolved by drive()'s event-scoped pre-pass (review finding c-i/c-ii); an ORPHAN response (no pending mint under that name) rides ext.google.unparsed rather than fabricating a dangling tool.done (finding c-iii)."
        },
        "inlineData": {
          "disposition": "handled",
          "note": "inlineDataBlock() routes by MIME prefix to the image/audio/file AgBlock arm (spec \u00a72)."
        },
        "executableCode": {
          "disposition": "handled",
          "note": "-> a `code` content.block (codeLanguage() maps the closed LANGUAGE_UNSPECIFIED|PYTHON enum defensively to a free string)."
        },
        "codeExecutionResult": {
          "disposition": "handled",
          "note": "-> a `code-result` content.block (codeOutcome() maps OUTCOME_OK/OUTCOME_DEADLINE_EXCEEDED/else->failed)."
        },
        "fileData": {
          "disposition": "handled",
          "note": "-> a `resource-link` content.block (uri + mimeType)."
        },
        "mediaResolution": {
          "disposition": "carried",
          "note": "FIXED (fixture-drift ratchet, google-adk-ratchet task): was NOT read anywhere in driveAdkPart \u2014 a genuinely-vanishing finding (Tenet-6). Now carried via the new unconditional unmapped-part-fields provider-raw content.block (checked at the TOP of driveAdkPart, before the kind-specific if-chain). SPEC \u00a78 item 23."
        },
        "videoMetadata": {
          "disposition": "carried",
          "note": "FIXED (fixture-drift ratchet): per genai's own doc ('should only be specified while the video data is presented in inline_data or file_data'), this field normally rides ALONGSIDE an already-handled inlineData/fileData part \u2014 but driveAdkPart's if-chain RETURNS as soon as it matches the primary kind, so a naive 'else' fallback placed AFTER the if-chain would never see this sibling field on a part whose primary content WAS handled. Fixed by checking unconditionally BEFORE the if-chain instead (same fix as mediaResolution). Verified with a fixture carrying BOTH inlineData and videoMetadata on the same part (index.test.ts). SPEC \u00a78 item 23."
        },
        "toolCall": {
          "disposition": "carried",
          "note": "FIXED (fixture-drift ratchet): 'Server-side tool call...the client is expected to echo this message back to the API' \u2014 a live-API/bidi construct, DISTINCT from the client-executed `functionCall` (echo-only semantics, no AgJSON tool-lifecycle concept models it). Carried via the same unmapped-part-fields provider-raw block rather than dropped."
        },
        "toolResponse": {
          "disposition": "carried",
          "note": "FIXED (fixture-drift ratchet): the client-supplied result of a server-side `toolCall` (paired with the entry above). Same carry."
        },
        "partMetadata": {
          "disposition": "carried",
          "note": "FIXED (fixture-drift ratchet): free-form per-part custom metadata ('name of a file/source...or a way to multiplex multiple Part streams' per genai's own doc). Same carry."
        },
        "audioTranscription": {
          "disposition": "carried",
          "note": "NEW in `@google/genai` 2.15.0 (2026-08-04 peer-bump ritual): 'Output only. The transcription of the audio part.' \u2014 a `Transcription` {text?, finished?, languageCode?, speakerLabel?, words?: WordInfo[]} (WordInfo {word?, startOffset?, endOffset?}, duration-string offsets; speakerLabel/words are themselves new at 2.15.0). Rides ALONGSIDE the audio `inlineData` part it transcribes \u2014 the same sibling situation as `videoMetadata`, so it joins the SAME unconditional unmapped-part-fields provider-raw carry (checked before the if-chain's early returns), with mirror + negative-control tests. Carried WHOLE rather than split: first-class text treatment (per the event-level `outputTranscription` `_meta['agjson/transcription']` stamp precedent) would orphan speakerLabel/words \u2014 recorded as an open spec-process candidate, not landed mechanically. Absent from all recorded fixtures/captures (no audio-out scenario in the corpus), so no golden-snapshot impact; live evidence comes with a future audio capture."
        },
        "mediaProcessing": {
          "disposition": "carried",
          "note": "NEW in `@google/genai` 2.20.0 (2026-09-02 peer-bump ritual, the adk 2.0.0 cohort): 'How the model processes this part's media for understanding.' \u2014 the `MediaProcessing` enum (MEDIA_PROCESSING_UNSPECIFIED = model-specific default | STATIC = fixed-rate frame extraction, all frames in context | AGENTIC = model-driven dynamic navigation). A REQUEST-side media-understanding hint that rides ALONGSIDE the inlineData/fileData part it qualifies \u2014 the identical sibling situation as mediaResolution/videoMetadata, so it joins the SAME unconditional unmapped-part-fields provider-raw carry at the top of driveAdkPart (checked BEFORE the if-chain's early returns; an else-fallback would never see it on a part whose primary kind already matched), typed JsonValue on AdkPart and routed through JsonValue.parse. Mirror tests: inlineData + mediaProcessing on the SAME part (file block AND the carry), alongside videoMetadata in ONE provider-raw block, beside a fileData part; negative control: a 2.17.1-shaped media part stays byte-identical (no provider-raw, no key). Absent from all recorded fixtures/captures (the corpus has no media-input scenario), so no golden-snapshot impact; the census norm-path a future capture would surface is [*].content.parts[*].mediaProcessing."
        }
      }
    },
    "eventField": {
      "$comment": "The `Event`/`LlmResponse` interface field names (the OFFICIAL `@google/adk`'s `dist/types/events/event.d.ts` + `dist/types/models/llm_response.d.ts`; `export interface Event extends LlmResponse`). Both interfaces' own fields are flattened into ONE inventory (17 from LlmResponse + 7 from Event, `id` only on Event in the official layout) \u2014 matching how the hand-typed `AdkEvent` interface itself makes no LlmResponse/Event split. Nested sub-object field names (e.g. AdkContent.role, EventActions' own members) are OUT OF SCOPE for this section \u2014 it covers only the TOP-LEVEL Event/LlmResponse field names, analogous to how the openai manifest's protocolItem section covers only top-level ModelItem discriminants, not fields nested inside a content/action sub-union. (Retargeted 2026-07-13 from `@iqai/adk` 0.1.22's bundled `declare class` pair \u2014 see the top-level $comment's RETARGETED note for the inventory delta.) RE-VERIFIED 2026-08-04 (adk 1.5.0 peer bump): the Event/LlmResponse OWN-field inventory is UNCHANGED 1.4.0->1.5.0 (all 24 members transfer; gate green with zero eventField findings). Honesty note on scope: 1.5.0's one event-plane wire addition \u2014 `isScratchpad?: boolean` on `CompactedEvent` (dist/types/events/compacted_event.d.ts, `CompactedEvent extends Event`; produced by the new anchored compactor, adk-js PR #470, with the new exported `isScratchpadEvent` guard) \u2014 lives on a SUBTYPE in its own file and is therefore INVISIBLE to this section's extraction (event.d.ts + llm_response.d.ts only); it can never appear as a member row here (the gate would flag it as removed-from-SDK). The facet carries the whole CompactedEvent projection (isCompacted/startTime/endTime/compactedContent/isScratchpad) via the event-level unmappedEvent provider-raw ledger as SESSION-REPLAY tolerance, per the candidateIndex off-inventory precedent: compactors run inside ContextCompactorRequestProcessor and rewrite session.events IN PLACE \u2014 a CompactedEvent is never yielded on the runner.runAsync boundary this facet normalizes, so the carry cannot fire on captured wire (no golden-snapshot impact; mirror + negative-control tests in index.test.ts). Also 1.5.0, no code impact: event.d.ts newly re-exports AF_FUNCTION_CALL_ID_PREFIX/generateClientFunctionCallId/populateClientFunctionCallId/getEventTokens (pure relocation of existing helpers, behavior identical \u2014 helper plane, not wire fields, outside this inventory either way); and the A2A-only behavioral fixes (errorCode surfaced in A2A metadata; contentless events now serialize content: undefined) touch only the a2a/ integration surface, which this facet does not normalize (runner.runAsync Events only) \u2014 no facet code path reads A2A frames, so no change needed or made. RE-VERIFIED 2026-09-02 (adk 1.6.0 -> 2.0.0 MAJOR peer bump; the facet's optional-peer range widened to >=1.0.0 <3): event.d.ts adds FOUR optional `Event` own-fields \u2014 `output?: unknown`, `route?: Route` (RouteKey | RouteKey[]; RouteKey = string | number | boolean), `nodeInfo?: NodeInfo` ({path?: string; outputFor?: string[]; messageAsOutput?: boolean}), `isolationScope?: string` \u2014 plus a non-serializable unique-symbol brand (`readonly [EVENT_SIGNATURE_SYMBOL]?: true`, set by createEvent / checked by isEvent) that is invisible to JSON and to this section's name extraction alike (correctly NOT a member). The inventory grows 25 -> 29 members: 18 LlmResponse own-fields (the 17 counted at the 1.3.0 retarget + interactionId since 1.4.0; llm_response.d.ts is byte-identical 1.6.0 -> 2.0.0) + 11 Event own-fields (the 7 prior + the four workflow fields). All four are stamped ONLY by the new workflow plane (dist/esm/workflow/node_runner.js sets output/route on node results and derives isolationScope per node; run_llm_agent_as_node.js stamps nodeInfo.messageAsOutput + the user event's isolationScope) and round-tripped through sessions/vertex_ai_session_service.js's event metadata \u2014 a plain LlmAgent runner.runAsync stream (this facet's ingestion boundary; the standing gemini37 echo/app-spec/thinking trio) never carries them, so each is disposed `carried` via the event-level unmappedEvent provider-raw ledger as workflow-plane / session-replay ingestion tolerance per the CompactedEvent/isScratchpad + interactionId precedents (mirror + 1.6.0-shaped byte-identical negative-control tests in index.test.ts; no golden-snapshot impact; transforms/allowlist/registry untouched because nothing fires on captured wire). Also 2.0.0, NESTED (outside this top-level-only section; recorded on the `actions` member's note): event_actions.d.ts adds `agentState?: Record<string, unknown>` + `endOfAgent?: boolean` as first-class EventActions members \u2014 both were ALREADY on the hand-typed contract and ledger-carried; agentState's contract type is corrected string -> JsonValue (2.0.0 writes an OBJECT, e.g. `{ input }` from node_runner.js) and routed through JsonValue.parse.",
      "sdkPackage": "@google/adk",
      "verifiedAt": "2.0.0",
      "members": {
        "id": {
          "disposition": "handled",
          "note": "Consumed as the turnKey() fallback (`ev.invocationId && ev.invocationId.length > 0 ? ev.invocationId : ev.id ?? \"adk\"`) when invocationId is absent \u2014 genuinely read, not merely typed."
        },
        "content": {
          "disposition": "handled",
          "note": "content.parts[] fan-out is the CORE of the entire facet (driveAdkPart per-part dispatch)."
        },
        "groundingMetadata": {
          "disposition": "handled",
          "note": "mapGroundingCitations() (per-segment citations attached to the ONE grounded text block's text.end, audit M22) + driveAdkTopLevel's `source`/`display.required` emission from groundingChunks/searchEntryPoint."
        },
        "partial": {
          "disposition": "handled",
          "note": "isPartial drives the entire streaming/aggregate-suppression state machine (\u00a78.3, the #1 ADK double-render quirk)."
        },
        "turnComplete": {
          "disposition": "handled",
          "note": "maybeCloseTurn's hasCompletion check (is_final_response gate, alongside finishReason/errorCode)."
        },
        "errorCode": {
          "disposition": "handled",
          "note": "turn.error `code` (when errorMessage is also set) and a mapFinishReason() fallback input; also gates hasCompletion in maybeCloseTurn."
        },
        "errorMessage": {
          "disposition": "handled",
          "note": "turn.error `message` \u2014 gates whether a completed turn with errorCode closes via closeTurnError (both set) vs closeTurnDone (errorCode alone, e.g. a non-fatal block reason)."
        },
        "interrupted": {
          "disposition": "handled",
          "note": "-> turn.abort{reason:'interrupted'} + marks the turn closed in the facet's own bookkeeping (audit M21, SPEC \u00a78 item 17: interruption terminals are never success)."
        },
        "customMetadata": {
          "disposition": "carried",
          "note": "Opaque per-event bag -> the event-level unmapped-fields provider-raw content.block (driveAdkTopLevel), alongside citationMetadata/branch/modelVersion (and the structural candidateIndex tolerance)."
        },
        "citationMetadata": {
          "disposition": "carried",
          "note": "Gemini Candidate.citationMetadata -> the event-level unmappedEvent provider-raw carry (a PRE-EXISTING ledger arm \u2014 the field was typed on AdkEvent and carried all along; this entry is new only because @iqai/adk's LlmResponse never declared it, so the old inventory could not see it). First-class AgCitation mapping from citationMetadata (as distinct from the already-handled groundingMetadata citations) is a plausible future slice."
        },
        "usageMetadata": {
          "disposition": "handled",
          "note": "mapUsage() -> AgUsage on turn.done (cumulative:false), SUMMED across the turn's non-partial events (2026-07-13, echo-gemini35 live-capture finding): ADK usageMetadata is per-LLM-call and one turn spans every round of the invocation \u2014 reading only the closing event dropped intermediate rounds' tokens (incl. thoughtsTokenCount, which only rides thinking rounds). The partial:true stream + partial:false aggregate re-send carry the SAME per-round usage, so non-partial-only counting counts each round exactly once."
        },
        "finishReason": {
          "disposition": "handled",
          "note": "mapFinishReason() -> the AgFinishReason superset (STOP/MAX_TOKENS/SAFETY family/MALFORMED_FUNCTION_CALL/OTHER/unknown)."
        },
        "modelVersion": {
          "disposition": "carried",
          "note": "NEW on the official-SDK retarget (2026-07-13): `@google/adk` 1.3.0 LlmResponse field absent from @iqai/adk's surface ('the model version that produced this response'). Genuinely optional response metadata \u2014 closed by the same opportunistic-carry precedent as candidateIndex/branch: added to AdkEvent + the event-level unmappedEvent provider-raw ledger. Absent from all recorded fixtures (they pre-date the field), so no golden-snapshot impact."
        },
        "interactionId": {
          "disposition": "carried",
          "note": "NEW in `@google/adk` 1.4.0 (llm_response.d.ts:83: 'The interaction ID returned by the model, if any'). Model-response identity metadata, populated only via the opt-in Interactions API path (GeminiParams.useInteractionsApi, also new in 1.4.0) \u2014 genuinely optional on this facet's ingestion boundary; closed by the same opportunistic-carry precedent as modelVersion: added to AdkEvent + driveAdkTopLevel's event-level unmappedEvent provider-raw ledger (2026-07-22 peer-bump ritual, with mirror test). Absent from all recorded fixtures and captures (the harness never enables useInteractionsApi), so no golden-snapshot impact. Caveat vs the author/timestamp noise rule: if an Interactions-API capture ever lands as a seed the field may ride every model event of a run \u2014 revisit toward a first-class correlation home then."
        },
        "liveSessionResumptionUpdate": {
          "disposition": "not-applicable",
          "note": "Live/bidi session control plane (`LiveServerSessionResumptionUpdate` \u2014 a session-resumption handle pushed by the LIVE server connection). Populated only on the live (`runLive`/bidi) path; never on the `runner.runAsync` Event stream this facet's ingestion boundary normalizes. Revisit if a live facet lands."
        },
        "goAway": {
          "disposition": "not-applicable",
          "note": "Live/bidi session control plane (`LiveServerGoAway` \u2014 imminent-disconnect notice from the LIVE server connection). Same runLive-only rationale as liveSessionResumptionUpdate."
        },
        "liveSessionId": {
          "disposition": "not-applicable",
          "note": "Live/bidi session correlation id, set only on the live connection path. Same runLive-only rationale as liveSessionResumptionUpdate."
        },
        "invocationId": {
          "disposition": "handled",
          "note": "turnKey()'s PRIMARY key (the whole-interaction run id / turn correlation key)."
        },
        "author": {
          "disposition": "silently-dropped",
          "note": "REAL FINDING (fixture-drift ratchet, google-adk-ratchet task): ALREADY typed on AdkEvent ('user' or the agent name) but NEVER READ anywhere in drive()/driveAdkTopLevel/maybeCloseTurn \u2014 a more insidious class of finding than a missing field, since the contract's presence gave a false impression of coverage. INVESTIGATED but DELIBERATELY NOT folded into the generic unmappedEvent carry (unlike branch): on the official @google/adk 1.3.0 `Event` interface `author?: string` is optional in the TYPE (it was required on the original @iqai/adk class), but the runner sets it on every event it appends in practice \u2014 so the empirical rationale is unchanged: present on EVERY real event, not an occasional payload. Empirically, carrying it here fired a provider-raw content.block on all 5 events of BOTH packages/e2e adk corpus scenarios (`author:\"spike\"`), breaking the recorded golden `*.agjson.json` snapshots and the cross-framework convergence assertions (adk's sequence gained content.block noise claude/openai's equivalent streams lack) \u2014 regenerating those cassette fixtures is outside this ratchet's boundary (facet+manifests+script+SPEC-\u00a78 only). A more precise, non-noisy home (e.g. an agent-identity field on message.start, for multi-agent transcript attribution) is a plausible FUTURE spec-process enhancement. index.test.ts documents this boundary with an explicit negative-control test."
        },
        "actions": {
          "disposition": "handled",
          "note": "transferToAgent -> handoff{kind:'transfer'}; escalate -> handoff{kind:'escalate'}; requestedAuthConfigs/requestedToolConfirmations -> hitl.ask{kind:'auth'|'approval'} folded into turn.done.outcome.paused.asks[] (audit M26, SPEC \u00a78 items 12+18); stateDelta -> state.delta; artifactDelta/renderUiWidgets/agentState/endOfAgent -> the pre-existing unmappedActions provider-raw carry (driveAdkTopLevel). 2.0.0: `agentState`/`endOfAgent` are now FIRST-CLASS on the official EventActions (event_actions.d.ts \u2014 'Workflow: a serialized node/agent state snapshot used for resumable checkpointing' / 'Workflow: marks that the emitting agent/workflow has reached the end of its execution for this invocation'); both remain ledger-carried via unmappedActions, with agentState's contract type corrected string -> JsonValue (2.0.0 writes an object; node_runner.js `{ input }`) and routed through JsonValue.parse \u2014 object-valued mirror test + legacy-string tolerance test + 1.6.0-shaped negative control."
        },
        "longRunningToolIds": {
          "disposition": "handled",
          "note": "Read in driveAdkPart to set `longRunning:true` on a matching tool.start."
        },
        "inputTranscription": {
          "disposition": "handled",
          "note": "Speech-to-text transcription of the user's audio input -> a text content.block stamped `_meta['agjson/transcription'] {role:'input'}` (driveAdkTopLevel). Was already typed AND handled on the hand-typed contract; this entry is new only because @iqai/adk's LlmResponse never declared it, so the old inventory could not see it. Caveat (2026-08-04, genai 2.15.0): the shared `Transcription` interface gained speakerLabel/words \u2014 this arm maps only `.text`, so those extras would drop if a live-path transcription ever reached this boundary (live/bidi-only in practice; sub-object shapes are outside this name-only ratchet). Revisit with a live facet."
        },
        "outputTranscription": {
          "disposition": "handled",
          "note": "Text-to-speech transcription of the model's audio output -> a text content.block stamped `_meta['agjson/transcription'] {role:'output'}` (driveAdkTopLevel). Same already-handled/newly-visible situation as inputTranscription \u2014 and the same 2026-08-04 genai-2.15.0 caveat: the shared `Transcription` shape's new speakerLabel/words are not read by this text-only arm."
        },
        "branch": {
          "disposition": "carried",
          "note": "FIXED (fixture-drift ratchet): ALREADY typed on AdkEvent ('multi-agent routing', format agent_1.agent_2.agent_3) but NEVER READ prior to this task \u2014 same insidious-finding class as `author`. Folded into the event-level unmappedEvent provider-raw carry. A more precise home (a branch/lineage field alongside handoff/subagent routing) is a plausible future spec-process enhancement. SPEC \u00a78 item 23."
        },
        "timestamp": {
          "disposition": "silently-dropped",
          "note": "REAL FINDING (fixture-drift ratchet, google-adk-ratchet task): ALREADY typed on AdkEvent ('ISO timestamp of the event' per this contract's own doc, though the installed SDK types it as a `number` \u2014 seconds since epoch, a shape divergence noted for a future runtime-adapter fix, out of scope for this name-only ratchet; true of BOTH the original @iqai/adk 0.1.22 class and the official @google/adk 1.3.0 interface) but NEVER READ anywhere. INVESTIGATED but DELIBERATELY NOT folded into the generic carry, for the SAME reason as `author`: `timestamp: number` is REQUIRED (non-optional) on the official `Event` interface \u2014 present on EVERY event. Carrying it here would fire on every native event in real usage, same noise/breakage risk empirically confirmed for `author`. DISCLOSED residual: SPEC.md's conformance item 13 documents a MORE PRECISE existing home for a source timestamp \u2014 the bare-key `_meta.timestamp` convention \u2014 but attaching it correctly would require choosing WHICH of this one native event's several fanned-out AgEvents anchors the stamp, a design decision beyond this ratchet's mechanical-carry scope. index.test.ts documents this boundary with an explicit negative-control test. Deeper rationale: wall-clock per-event timestamps are the SAME nondeterminism class the AgJSON base envelope deliberately omits (the deleted ts? field, audit M52 / INV-FOLD) \u2014 carrying it as content would reintroduce nondeterministic snapshots even if the field were rare."
        },
        "output": {
          "disposition": "carried",
          "note": "NEW in `@google/adk` 2.0.0 (event.d.ts: 'Workflow: the structured output produced by the emitting node, if any' \u2014 `output?: unknown`, mirroring adk-python Event.output; node_runner.js sets it from the child node's return value). Stamped ONLY by the 2.0.0 workflow plane (dist/esm/workflow/*) and round-tripped by vertex_ai_session_service.js; a plain LlmAgent runner.runAsync stream never carries it, so it NEVER appears on the standing gemini37 trio's wire \u2014 carried via the event-level unmappedEvent provider-raw ledger (driveAdkTopLevel) as workflow-plane / session-replay ingestion tolerance per the CompactedEvent/isScratchpad + interactionId precedents (present-check, JsonValue.parse at the boundary; mirror + byte-identical negative-control tests). Typed JsonValue on AdkEvent (any JSON value incl. null at this boundary). Future census norm-path: [*].output (leaf paths under it when object-valued)."
        },
        "route": {
          "disposition": "carried",
          "note": "NEW in `@google/adk` 2.0.0 (event.d.ts: 'Workflow: the route key(s) emitted by a routing node, used by the graph to select the matching outgoing edge(s)' \u2014 `route?: Route`, Route = RouteKey | RouteKey[], RouteKey = string | number | boolean; a single key fires one branch, an array fires every branch whose route matches any listed key). Stamped ONLY by the 2.0.0 workflow plane (dist/esm/workflow/*) and round-tripped by vertex_ai_session_service.js; a plain LlmAgent runner.runAsync stream never carries it, so it NEVER appears on the standing gemini37 trio's wire \u2014 carried via the event-level unmappedEvent provider-raw ledger (driveAdkTopLevel) as workflow-plane / session-replay ingestion tolerance per the CompactedEvent/isScratchpad + interactionId precedents (present-check, JsonValue.parse at the boundary; mirror + byte-identical negative-control tests). `false`/`0` are valid RouteKeys, so the ledger arm present-checks rather than truthiness-checks (tested). Future census norm-paths: [*].route (scalar) / [*].route[*] (multi-route array)."
        },
        "nodeInfo": {
          "disposition": "carried",
          "note": "NEW in `@google/adk` 2.0.0 (event.d.ts: 'Workflow: provenance of the emitting node' \u2014 `nodeInfo?: NodeInfo` {path?: the producing node path e.g. `wf.child.0`; outputFor?: string[] = the node paths this event's output serves as output for, emitting node first then delegating ancestors (useAsOutput); messageAsOutput?: whether the textual content is promoted to the node's structured output}; run_llm_agent_as_node.js stamps messageAsOutput, runner.js reads `.path` to detect node-produced events). Stamped ONLY by the 2.0.0 workflow plane (dist/esm/workflow/*) and round-tripped by vertex_ai_session_service.js; a plain LlmAgent runner.runAsync stream never carries it, so it NEVER appears on the standing gemini37 trio's wire \u2014 carried via the event-level unmappedEvent provider-raw ledger (driveAdkTopLevel) as workflow-plane / session-replay ingestion tolerance per the CompactedEvent/isScratchpad + interactionId precedents (present-check, JsonValue.parse at the boundary; mirror + byte-identical negative-control tests). Carried as a WHOLE object (never split). Future census norm-paths: [*].nodeInfo.path, [*].nodeInfo.outputFor[*], [*].nodeInfo.messageAsOutput."
        },
        "isolationScope": {
          "disposition": "carried",
          "note": "NEW in `@google/adk` 2.0.0 (event.d.ts: \"Workflow: scope tag used to isolate multi-agent conversations so peer scopes don't see each other's events\" \u2014 `isolationScope?: string`, e.g. `<nodePath>@<runId>`; derived per node in node_runner.js, stamped on the node's user event in run_llm_agent_as_node.js, and honoured by content_request_processor.js when assembling the LLM request's history). Stamped ONLY by the 2.0.0 workflow plane (dist/esm/workflow/*) and round-tripped by vertex_ai_session_service.js; a plain LlmAgent runner.runAsync stream never carries it, so it NEVER appears on the standing gemini37 trio's wire \u2014 carried via the event-level unmappedEvent provider-raw ledger (driveAdkTopLevel) as workflow-plane / session-replay ingestion tolerance per the CompactedEvent/isScratchpad + interactionId precedents (present-check, JsonValue.parse at the boundary; mirror + byte-identical negative-control tests). A more precise home (a scope/lineage field alongside `branch` and handoff routing) is a plausible future spec-process enhancement, same as branch. Future census norm-path: [*].isolationScope."
        }
      }
    }
  }
}
