# @silverprotocol/google-adk

AgJSON normalizer for the **official Google Agent Development Kit** —
[`@google/adk`](https://github.com/google/adk-js) (ADK `Event` / Gemini
`Content.parts[]` → `AgEvent[]`). It **sits on top of your ADK runner** and
translates the events it yields into framework-neutral AgJSON; it doesn't
replace the SDK you already use.

## Install

```sh
npm install @silverprotocol/google-adk @google/adk
```

`@silverprotocol/core` is a required peer (pulled in automatically). `@google/adk`
is the Google ADK itself — you need it to *produce* the events. (The normalizer
guards ADK `Event`s structurally, so if you only feed it already-captured events
it's just a type-level peer.)

<!-- compat:begin -->
<!-- generated by scripts/render-compat.mjs — do NOT edit by hand.
     Facts live in package.json#peerDependencies + sdk-surface.json#verified;
     edit those, then re-run `node scripts/render-compat.mjs`. -->
## Upstream compatibility

Supported peer range: `@google/adk` `>=1.0.0 <3` (npm-enforced, optional — this package never imports the SDK).

Wire shapes outside the verified set never crash the normalizer, and the
fixture-drift gate (`scripts/check-fixture-drift.mjs`) turns every newly-appeared
upstream field into a tracked triage disposition rather than an unnoticed drop —
carried losslessly where a carry channel exists (see `sdk-surface.json` for this
facet's per-field dispositions, including its disclosed `silently-dropped` gaps).

| `@google/adk` | verified | with | evidence |
| --- | --- | --- | --- |
| `2.0.0` | 2026-09-02 | 0.5.4 | @google/adk 1.6.0->2.0.0 MAJOR (npm 2026-08-21; nightly [peer-drift] typescript-sdk#22) + @google/genai 2.17.1->2.20.0 - tarball d.ts audit independently re-derived: every listed breaking change (LLMAgentWrapper removal, InvocationContext.agent optional, BaseAgent extends BaseNode, Sequential/Parallel/Loop deprecation, rerunOnResume, workflow engine #588) lives on the new workflow/node plane; LlmResponse byte-identical; Event gains four optional workflow-plane fields (output/route/nodeInfo/isolationScope, stamped only by workflow/*.js and vertex_ai_session_service round-trips - never on a plain LlmAgent runAsync stream) -> carried into the event-level provider-raw ledger (CompactedEvent/interactionId precedent); EventActions gains agentState (object; contract widened string->JsonValue, JsonValue.parse'd) + endOfAgent (already carried); genai Part gains ONE field, mediaProcessing (request-side media hint; MEDIA_PROCESSING_UNSPECIFIED\|STATIC\|AGENTIC) -> unconditional top-of-part carry beside mediaResolution/videoMetadata; FinishReason (18), Content, Candidate, ThinkingConfig/ThinkingLevel, UsageMetadata, FunctionCall/FunctionResponse IDENTICAL. Dependency shape: @modelcontextprotocol/sdk and the five @mikro-orm drivers + @google-cloud/storage + GCP OTel exporters became OPTIONAL peers loaded lazily (591->172 packages) - consumers using MCPToolset must install @modelcontextprotocol/sdk themselves; the lockfile's mikro-orm/sqlite3/tar subtree left (17 dev-only dependabot alerts closed); adm-zip stays ^0.5.17 (google/adk-js#601; workspace override to ^0.6.0) and the 0.205.0 otlp-http exporters still exact-pin @opentelemetry/core 2.1.0 (override ^2.8.0). Capture agent (InMemoryRunner/LlmAgent/MCPToolset/runAsync) typechecks and runs unchanged. LIVE: the standing gemini-3.7-flash trio RE-CAPTURED at 2.0.0/2.20.0 (echo/app-spec/thinking; app-spec first - it drives MCPToolset through the new optional-peer loader): census ZERO drops / ZERO new fields on all three, thinking-gemini37 still yields thought:true parts + signatures. Peer range widened >=1.0.0 <2 -> <3 (floor kept: 1.x and 2.x Event shapes normalize through one facet; vercel-ai's two-major range is the precedent); e2e pins adk 2.0.0 + genai 2.20.0 exact; stale exclusions pruned. Nightly leg 'force @google/adk@2.0.0' was already green (run 33564341176). Consumers compiling with skipLibCheck:false against adk 2.0.0 should know its root barrel type-imports the now-optional @google-cloud/storage (TS2307) - upstream shape, not ours. |
| `1.6.0` | 2026-08-19 | 0.5.1 | gemini-3.7-flash day-6 LIVE validation trio (GA 2026-08-13): echo + app-spec (MCP structuredContent/_meta.ui carry re-attested) + thinking (FIRST real Gemini thought:true capture — thinkingLevel scenario knob -> thinkingConfig.includeThoughts, 2 thought parts + signatures -> reasoning.start/delta/end + reasoning.opaque). All three enrolled as replay seeds at capture time; census clean after [*].content.parts[*].thought triage (structurally-consumed flag, [*].partial precedent). New finish-reason mappings (UNEXPECTED_TOOL_CALL, TOO_MANY_TOOL_CALLS + lossy message.metadata raw carry) ship in the same cohort. |
| `1.6.0` | 2026-08-15 | 0.4.4 | genai 2.17.0->2.17.1 PATCH study (adk itself unchanged at 1.6.0): Part interface byte-identical (tarball d.ts diff) - wire-no-op; partKind section verifiedAt -> 2.17.1, e2e genai pin 2.17.1. |
| `1.6.0` | 2026-08-13 | 0.4.2 | genai 2.16.0->2.17.0 MINOR study (adk itself unchanged at 1.6.0): Part interface zero-delta (15 fields, tarball d.ts diff + drift-gate-verified post-install) — wire-no-op; partKind section verifiedAt -> 2.17.0, e2e genai pin 2.17.0. |
| `1.6.0` | 2026-08-08 | 0.4.1 | wire-no-op minor (parked from the 2026-08-08 ai-trio slice, studied same day): full d.ts inventory diff 1.5.0->1.6.0 + genai 2.15.0->2.16.0 against the drift gate's own consumed surfaces — genai Part 15 fields unchanged (the surface that forced the 1.5.0 study via audioTranscription: zero delta this time), adk Event 7 own fields unchanged, LlmResponse 18 fields unchanged. 1.6.0's additions are all non-wire (a2a/auth, get_user_choice_tool + request_input_tool — registerable tools whose invocations ride the EXISTING functionCall/functionResponse envelope — crypto/error/output_schema utils). e2e pins bumped exact (adk 1.6.0, genai 2.16.0), single-copy lockfile resolution verified, 918 tests incl. corpus replay + fold gates green. Nightly's forced-latest adk leg has been green all week (no [peer-compat] ever filed). No new corpus scenario demanded — no wire surface changed to capture. |
| `1.5.0` | 2026-08-04 | 0.3.9 | Live captures landed (echo-gemini36 + app-spec-gemini36 @ gemini-3.6-flash, adk 1.5.0 / genai 2.15.0): replay + census + INV-MSG fold gates all green with ZERO new census findings — the 1.5.0 wire at this facet's runner.runAsync boundary matches the tarball audit exactly. audioTranscription remains synthetic-only (no native-audio-output scenario exists in the corpus; disclosed in the carry entry above). |
| `1.5.0` | 2026-08-04 | 0.3.9 | 1.4.0->1.5.0 (+genai 2.12.0->2.15.0) tarball-diff audit + synthetic-frame tests: Event/LlmResponse own fields UNCHANGED (24-member eventField inventory transfers; gate green); the two real wire additions both carried — (1) CompactedEvent gains isScratchpad (anchored compactor scratchpad, PR #470; subtype file, outside the ratcheted inventory — whole projection carried via the unmappedEvent provider-raw ledger as session-replay tolerance, true/false/absent + author-suppression mirror tests, never reachable on the runAsync boundary), (2) genai Part gains audioTranscription (FIRST new part kind since the 14-member ratchet; carried whole via the unmapped-part-fields provider-raw block per the videoMetadata sibling precedent, mirror + 2.12.0-shaped byte-identical negative controls; Reducer folds clean, no parks). Non-wire: event.d.ts re-export relocation (AF_FUNCTION_CALL_ID_PREFIX/generateClientFunctionCallId/populateClientFunctionCallId/getEventTokens — behavior identical) and A2A-only fixes (errorCode metadata, contentless content: undefined — the facet has no A2A surface). Pin bumped; live re-capture pending in this cohort's capture step. |
| `1.4.0` | 2026-07-25 | 0.3.6 | FIRST adk MCP-Apps capture (workspace#2): live gemini-3.6-flash render_card via the official MCPToolset — corpus app-spec-gemini36 landed as a CI-standing replay seed; the census caught functionResponse.response's MCP-Apps SIBLINGS (structuredContent + _meta.ui.resourceUri) silently dropped by the content-array branch — FIXED: functionResponseToToolDoneFields now carries both onto tool.done's first-class §2.1 channels (converging with the openai facet's extractStructuredContent precedent), mirror tests added, cassette regenerated through the fixed facet, replay + census gates green |
| `1.4.0` | 2026-07-22 | 0.3.5 | LIVE capture: echo-gemini36 standing seed REFRESHED at 1.4.0 (gemini-3.6-flash, genai 2.12.0 underneath) — replay + census gates green (zero untriaged drops, zero new fields); interactionId absent from wire as expected (harness never enables useInteractionsApi) |
| `1.4.0` | 2026-07-22 | 0.3.5 | fixture suite + drift gate + full 1.3.0->1.4.0 d.ts diff audit: exactly ONE new Event/LlmResponse wire field — interactionId (opt-in Interactions API identity; carried via the unmappedEvent provider-raw ledger per the modelVersion precedent, with mirror test) — all other deltas config/tooling-plane (hasThoughts/pruneThoughts helpers, RunnerConfig app/resumabilityConfig, CompositeSessionKey refactor). genai 1.52.0->2.x MAJOR diffed clean: Part/Content/FunctionCall/FunctionResponse/GenerateContentResponseUsageMetadata byte-identical at 2.12.0 AND 2.13.0 (partKind verifiedAt advanced to the installed 2.12.0). google/adk-js#478 (fix for the role-less-newMessage 400 we filed as #475) verified SHIPPED in 1.4.0's dist (runner.js defaults role to 'user', esm :113-114) AND live-confirmed: a deliberately role-less newMessage survived a full gemini-3.6-flash tool-loop replay |
| `1.3.0` | 2026-07-22 | 0.3.3 | day-one gemini-3.6-flash validation: live text + tool-loop e2e clean, census-grade capture landed as standing replay seed (corpus echo-gemini36) with native field-set diffed against the gemini-3.5-flash baseline — zero new wire fields, all dispositions transfer, replay gate green |
| `1.3.0` | 2026-07-13 | 0.3.3 | LIVE conformance capture: echo-gemini35 corpus (gemini-3.5-flash, official MCPToolset over Streamable HTTP) — landed as a standing replay/census seed; census caught + fixed per-turn usage summation (intermediate rounds' usageMetadata incl. thoughtsTokenCount now summed into turn.done) |
| `1.3.0` | 2026-07-13 | 0.3.3 | drift-gate eventField re-seed against the official interfaces (24 members) + e2e harness migration off @iqai/adk; live conformance capture pending |
| `1.3.0` | 2026-07-08 |  | LIVE captures: multi-turn + single-tool-call + text-only + tool-error corpus (gemini-2.5-flash) — captured directly on the public typescript-sdk mirror by a parallel migration effort; adopted into the workspace 2026-07-13 (native+provenance verbatim, agjson/coverage regenerated through the current facet) and landed as standing replay/census seeds |
| `1.3.0` | 2026-07-07 | 0.3.1 | live validation: launch/validation/e2e-adk-google*.mjs (text + tool turns; upstream google/adk-js#475 diagnosed to root cause and filed) |
<!-- compat:end -->

## Usage

```ts
import { LlmAgent, InMemoryRunner } from "@google/adk";
import { createAdkNormalizer } from "@silverprotocol/google-adk";

// Build your ADK agent + runner as usual — this package normalizes what it emits.
const agent = new LlmAgent({
  name: "assistant",
  model: "gemini-3.7-flash",
  instruction: "Use the echo tool.",
});
const runner = new InMemoryRunner({ agent });
const session = await runner.sessionService.createSession({
  appName: runner.appName,
  userId: "user-1",
});

const n = createAdkNormalizer();
const agEvents = [];
for await (const native of runner.runAsync({
  userId: session.userId, // must match the session you created
  sessionId: session.id,
  newMessage: { role: "user", parts: [{ text: "call the echo tool" }] }, // role is REQUIRED: adk-js drops role-less turns from tool-loop replays (400)
}))
  agEvents.push(...n.push(native)); // one ADK Event → 0+ AgEvents
agEvents.push(...n.flush());        // seal anything still open
```

`push()` returns the `AgEvent[]` synthesized from each native ADK `Event`;
`flush()` drains buffered end-of-stream state. Any async/sync iterable of ADK
`Event`s works — a live `runAsync()` run, or events you captured earlier.

Then fold the resulting `AgEvent`s into messages and turns with
`@silverprotocol/core`'s `reduce()` — the same client code regardless of which
framework produced the stream.

Spec: [silverprotocol.io/AgJSON](https://silverprotocol.io/AgJSON) — canonical
in [silverprotocol/AgJSON](https://github.com/silverprotocol/AgJSON); wire
version `1.0.0-draft.1`.
