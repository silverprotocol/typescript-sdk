{
  "name": "@silverprotocol/openai-agents",
  "version": "0.3.1",
  "description": "AgJSON normalizer for the OpenAI Agents SDK (RunItemStreamEvent / Responses streaming → AgEvent[]) — fixture-tested.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/silverprotocol/typescript-sdk.git",
    "directory": "packages/openai-agents"
  },
  "homepage": "https://github.com/silverprotocol/typescript-sdk#readme",
  "bugs": { "url": "https://github.com/silverprotocol/typescript-sdk/issues" },
  "publishConfig": { "access": "public" },
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "src", "!src/**/*.test.ts", "!dist/**/*.test.*"],
  "scripts": {
    "build": "tsc -b --force",
    "prepack": "rm -rf dist && tsc -b --force",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@silverprotocol/core": "workspace:*"
  },
  "peerDependencies": {
    "@openai/agents": ">=0.2.0 <0.13"
  },
  "peerDependenciesMeta": {
    "@openai/agents": {
      "optional": true
    }
  }
}
