{
  "name": "tool-error-gpt6sol",
  "prompt": "Call the fail tool and report what error it returned.",
  "mcpServers": [
    { "key": "errsrv", "kind": "error" }
  ],
  "steer": "You MUST call the tool named fail. The tool will return an error — report the error code and message exactly as returned. Do not skip the tool call."
}
