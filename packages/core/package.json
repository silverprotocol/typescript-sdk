{
  "name": "@silverprotocol/core",
  "version": "0.0.0",
  "description": "AgJSON core — the typed schema (AgentInput / AgentEvent / AgentMessage), the Normalizer interface, and the normative reduce().",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "jsonata": "^2.0.0",
    "zod": "^4.0.0"
  }
}
